package week5

/**
  match xs {
    case List() => throw new error
    case List(x) => list()
    case y :: ys => y::list(ys)
  }
  the match works in order

  xs match {
    case list() => xs
    case y

  removeAt

    zs = list()
    removeIter(zs,n,xs)
    if n < 0 error
    if n == 0 zs:::xs
    if n > 0 removeIter(z::zs,n-1,xs.tail)
    )



match xs {
    case list() => list()
    case
    case y::ys => removeAt(n-1,ys)


  }


  */